- name: Bootstrap Local Settings
  hosts: localhost
  connection: local
  roles:
    - bootstrap-local

- name: Bootstrap Required Users
  hosts: all_kube_nodes
  remote_user: root
  gather_facts: no
  roles:
    - bootstrap-users